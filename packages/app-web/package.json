{
  "name": "app-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:client-sdk": "bun run --cwd ../client-sdk build",
    "pree2e": "bun run build:client-sdk",
    "e2e": "playwright test",
    "test:e2e": "bun run e2e",
    "predev": "bun run build:client-sdk",
    "dev": "bun --hot src/servers/devServer.ts",
    "prebuild": "bun run build:client-sdk",
    "build": "rm -rf dist && bun build ./src/index.html --outdir=dist --sourcemap --target=browser --minify --define:process.env.NODE_ENV='production' --env='BUN_PUBLIC_*' --public-path=/ && bun scripts/buildStaticAssets.ts && bun scripts/buildServiceWorker.ts",
    "prestart": "bun run build:client-sdk",
    "start": "bun src/servers/devServer.ts",
    "test": "bun test scripts --max-concurrency=1"
  },
  "dependencies": {
    "@tearleads/sqlite-worker": "workspace:*",
    "app": "workspace:*",
    "react-dom": "catalog:react"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "@types/bun": "catalog:",
    "@types/react-dom": "catalog:react"
  }
}
