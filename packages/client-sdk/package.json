{
  "name": "@tearleads/client-sdk",
  "version": "0.1.0",
  "description": "React-free Tearleads runtime with atomic document/container writes, placement-aware persistence notifications, required revision-CAS host adapters, unified device-first handles, signed-name group mutations, verified organization policy history details without persisted names, verified keying, held-descendant re-citation, sync, encrypted blob storage, root operator identity/organization billing and data usage lookups, typed registration refusals, and teardown predicates for host diagnostics.",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./sqlite": {
      "types": "./dist/sqlite.d.ts",
      "default": "./dist/sqlite.js"
    },
    "./testing": {
      "types": "./dist/data/trustedUserIdentity/testFixtures.d.ts",
      "default": "./dist/data/trustedUserIdentity/testFixtures.js"
    }
  },
  "scripts": {
    "build": "sh scripts/build.sh",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "bun test src --max-concurrency=4 --pass-with-no-tests"
  },
  "dependencies": {
    "@tearleads/api-client": "workspace:*",
    "@tearleads/crypto": "workspace:*",
    "@tearleads/encoding": "workspace:*",
    "@tearleads/loro": "workspace:*",
    "@tearleads/sqlite-worker": "workspace:*",
    "@tearleads/validators": "workspace:*",
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "@tearleads/test-utils": "workspace:*",
    "@types/bun": "catalog:"
  }
}
