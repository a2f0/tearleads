export const stripeReturnUrlOriginRefinement = {
  description:
    "returnUrl parses as HTTP(S) and its origin matches the configured app origins",
  id: "request.stripe-return-url-origin",
} as const;
