// Single source of truth for the System Monitor glyph. Shared by the footer
// tray launcher button (windowed mode) and the routed nav item (compact/mobile
// mode) so the two never drift apart.
export { ListMagnifyingGlassIcon as SystemMonitorIcon } from "@phosphor-icons/react/dist/csr/ListMagnifyingGlass";
