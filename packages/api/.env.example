PORT=5001
NODE_ENV=development
# Secret for signing JWTs. Generate one with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret
# API key for OpenRouter. Get from https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key
