{
  "name": "@symcrypt/api-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun scripts/buildApiCliExecutable.ts",
    "migrate": "bun src/index.ts migrate",
    "test": "bun test src"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.1049.0",
    "@symcrypt/api-shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:"
  }
}
