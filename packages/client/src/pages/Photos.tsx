/**
 * Photos page - re-exports from modular implementation.
 *
 * This file re-exports the Photos components from the photos-components/ directory
 * for backward compatibility with existing imports.
 */

export type { PhotoInfo, PhotosProps, PhotoWithUrl } from './photos-components';
export { Photos, PhotosPage } from './photos-components';
