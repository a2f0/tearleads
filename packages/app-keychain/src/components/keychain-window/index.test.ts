import { describe, expect, it } from 'vitest';
import { KeychainWindow } from './index';

describe('keychain-window/index', () => {
  it('exports KeychainWindow', () => {
    expect(KeychainWindow).toBeDefined();
  });
});
