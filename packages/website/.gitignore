# Favicon assets generated from @tearleads/ui by scripts/buildWebImages.sh
# (runs on predev/prebuild). Regenerated on every dev/build, so not committed.
/public/favicon.svg
/public/favicon.ico
/public/apple-touch-icon.png

# Screenshot gallery assets staged from `.screenshots/` by
# scripts/buildScreenshots.ts (runs on predev/prebuild). Regenerated on every
# dev/build, so not committed.
/public/screenshot-gallery/
