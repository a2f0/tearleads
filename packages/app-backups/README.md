# @tearleads/app-backups

Backups page and floating window components for Tearleads.

## Development

```bash
# Build
pnpm --filter @tearleads/app-backups build

# Test
pnpm --filter @tearleads/app-backups test

# Test with coverage
pnpm --filter @tearleads/app-backups test:coverage
```
