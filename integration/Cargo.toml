[package]
name = "integration"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
client = { path = "../client" }
server = { path = "../server" }
protocol = { path = "../protocol" }
transport = { path = "../transport" }
