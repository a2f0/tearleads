# Screen configuration for tuxedo workspaces
# Enables mouse wheel scrolling through tmux by disabling alternate screen

# Start shell as login shell (-$SHELL) so nvm and other tools initialize properly
shell -$SHELL

# Large scrollback buffer (10k lines)
defscrollback 10000

# Disable alternate screen - allows tmux to capture scrollback
# ti@ disables the "enter alternate screen" sequence
# te@ disables the "exit alternate screen" sequence
# Match both xterm variants and tmux-256color (used by tuxedo.sh -T flag)
termcapinfo xterm* ti@:te@
termcapinfo tmux* ti@:te@
termcapinfo screen* ti@:te@
