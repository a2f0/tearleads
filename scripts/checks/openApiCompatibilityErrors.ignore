# #1917 is a greenfield flag-day contract change with no compatibility shim.
# Remove this entry in the next PR after main contains the required field.
POST /documents/{documentId}/sync added the new required request property `outgoingUpdates/items/plaintextHash`
