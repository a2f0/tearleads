# Intentional greenfield flag-day: signed principal grant commitments and their
# complete grant projections are required on every principal-policy write.
# Remove these entries after this contract reaches the default branch.
POST /auth/register added the new required request property `initialAdminGroup/initialGroupPolicy/grants`
POST /auth/register added the new required request property `initialAdminGroup/initialGroupPolicy/state/grantCount`
POST /auth/register added the new required request property `initialAdminGroup/initialGroupPolicy/state/grantRoot`
POST /auth/register added the new required request property `initialMemberGroup/initialGroupPolicy/grants`
POST /auth/register added the new required request property `initialMemberGroup/initialGroupPolicy/state/grantCount`
POST /auth/register added the new required request property `initialMemberGroup/initialGroupPolicy/state/grantRoot`
POST /auth/register added the new required request property `initialOrganizationPolicy/grants`
POST /auth/register added the new required request property `initialOrganizationPolicy/state/grantCount`
POST /auth/register added the new required request property `initialOrganizationPolicy/state/grantRoot`
POST /organizations added the new required request property `initialAdminGroup/initialGroupPolicy/grants`
POST /organizations added the new required request property `initialAdminGroup/initialGroupPolicy/state/grantCount`
POST /organizations added the new required request property `initialAdminGroup/initialGroupPolicy/state/grantRoot`
POST /organizations added the new required request property `initialMemberGroup/initialGroupPolicy/grants`
POST /organizations added the new required request property `initialMemberGroup/initialGroupPolicy/state/grantCount`
POST /organizations added the new required request property `initialMemberGroup/initialGroupPolicy/state/grantRoot`
POST /organizations added the new required request property `initialOrganizationPolicy/grants`
POST /organizations added the new required request property `initialOrganizationPolicy/state/grantCount`
POST /organizations added the new required request property `initialOrganizationPolicy/state/grantRoot`
POST /organizations/{organizationId}/groups added the new required request property `initialGroupPolicy/grants`
POST /organizations/{organizationId}/groups added the new required request property `initialGroupPolicy/state/grantCount`
POST /organizations/{organizationId}/groups added the new required request property `initialGroupPolicy/state/grantRoot`
PUT /principals/{principalType}/{principalId}/policy added the new required request property `grants`
PUT /principals/{principalType}/{principalId}/policy added the new required request property `state/grantCount`
PUT /principals/{principalType}/{principalId}/policy added the new required request property `state/grantRoot`
