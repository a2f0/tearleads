# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the new required request property `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register the `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /auth/register added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /blobs/{blobId}/attachment-bindings added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /blobs/{blobId}/attachment-bindings added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /blobs/{blobId}/attachment-bindings the `contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /blobs/{blobId}/attachment-bindings the `contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /blobs/{blobId}/attachment-bindings added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /containers/with-metadata-document added the new required request property `metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /containers/with-metadata-document added the new required request property `metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /containers/with-metadata-document the `metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /containers/with-metadata-document the `metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /containers/with-metadata-document added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `metadataDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents the `contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents the `contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/link added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/link added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/link the `contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/link the `contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/link added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/sync added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/sync added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/sync the `contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/sync the `contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/sync added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/unlink added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/unlink added the new required request property `contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/unlink the `contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/unlink the `contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /documents/{documentId}/unlink added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/iv`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the new required request property `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappingMetadata/suite`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's maxLength was set to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations the `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappedKey` request property's minLength was increased from `1` to `64`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialOrganizationMetadataContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialOrganizationMetadataContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialOrganizationProfileDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialOrganizationProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRootMetadataDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRootMetadataDocument/initialSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRosterProfileContainer/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRosterProfileContainer/metadataDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRosterProfileDocument/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialRosterProfileDocument/initialSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialSystemContainers/items/initialMetadataSync/contentKeyBundle/targets/items/wrappedKey`
# #2340: greenfield envelope contract; remove once main contains the strict schemas.
POST /organizations added the pattern `^[A-Za-z0-9+/]{64}$` to the request property `initialSystemContainers/items/metadataDocument/contentKeyBundle/targets/items/wrappedKey`
