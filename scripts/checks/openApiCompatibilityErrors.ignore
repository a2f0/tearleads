# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /auth/register added the new required request property `initialOrganizationMetadataContainer/initialMetadataSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /auth/register added the new required request property `initialOrganizationProfileDocument/initialSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /auth/register added the new required request property `initialRootMetadataDocument/initialSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /auth/register added the new required request property `initialRosterProfileContainer/initialMetadataSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /auth/register added the new required request property `initialRosterProfileDocument/initialSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /auth/register added the new required request property `initialSystemContainers/items/initialMetadataSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /documents/{documentId}/sync added the new required request property `supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /organizations added the new required request property `initialOrganizationMetadataContainer/initialMetadataSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /organizations added the new required request property `initialOrganizationProfileDocument/initialSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /organizations added the new required request property `initialRootMetadataDocument/initialSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /organizations added the new required request property `initialRosterProfileContainer/initialMetadataSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /organizations added the new required request property `initialRosterProfileDocument/initialSync/supportsPullPagination`
# #2096 greenfield pagination flag day; remove after this contract reaches main.
POST /organizations added the new required request property `initialSystemContainers/items/initialMetadataSync/supportsPullPagination`
