[Unit]
Description=Tearleads API
After=network.target postgresql.service redis-server.service{% if garage_enabled | default(false) | bool %} garage.service{% endif +%}
Wants=postgresql.service redis-server.service{% if garage_enabled | default(false) | bool %} garage.service{% endif +%}

[Service]
Type=simple
User={{ server_user | default('deploy') }}
WorkingDirectory=/opt/tearleads
EnvironmentFile=/etc/tearleads/api.env
ExecStart=/opt/tearleads/bin/tearleads-api
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal
SyslogIdentifier=tearleads-api

[Install]
WantedBy=multi-user.target
