server {
    listen 80;
{% if item == 'www' %}
    server_name {{ domain }} www.{{ domain }};
{% else %}
    server_name {{ item }}.{{ domain }};
{% endif %}

    root /var/www/{{ item }};
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Serve .mjs files with correct MIME type for ES modules
    location ~* \.mjs$ {
        types { application/javascript mjs; }
        default_type application/javascript;
    }

    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Cross-Origin-Embedder-Policy "require-corp" always;
    # CSP style hashes generated by: packages/website/scripts/generate-csp-hashes.sh
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; style-src 'self' 'unsafe-hashes' 'sha256-Bp8uiZj5gsDTIj8WTP7ta4fWYoaXKibtP2WrHnfM7Is=' 'sha256-eyEQIkqjJt2vT6Kt1pkSUhwTpB67WcyjJnghw+gdf54=' 'sha256-SVYkOSpXsFWz1rGuN7ZKkcC7PX154CiUZJ4JRtD9D6I='; img-src 'self' data:; connect-src 'self' https://api.{{ domain }}; object-src 'none'; frame-ancestors 'self';" always;
}
