set -e

pnpm lint
pnpm lint:scripts
pnpm exec tsc -b
pnpm build

if ! command -v bundle >/dev/null 2>&1; then
  echo "Error: bundle is not installed. Please install Ruby and Bundler."
  exit 1
fi
pnpm lint:rubocop
