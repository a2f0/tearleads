# tearleads-api-client-rs

Typed Rust client foundations for API v2 contracts.

This crate provides:
- canonical Connect/gRPC-web RPC path helpers for `tearleads.v2` services
- request context/header normalization (auth + organization)
- re-exports for generated v2 contract modules
