# tearleads-api-client-wasm

WASM bindings for API v2 Rust client foundations.

This crate exports:
- Connect base URL normalization
- canonical v2 RPC path helpers for Wave 1A admin methods
- request header construction from auth + organization context
