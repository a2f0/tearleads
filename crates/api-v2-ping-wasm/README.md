# `tearleads-api-v2-ping-wasm`

WASM bindings for the API v2 ping contract.

Exports:

- `v2_ping_path()` -> canonical endpoint path (`/v2/ping`)
- `parse_v2_ping_json(json)` -> validates/parses JSON payload
- `parse_v2_ping_value(value)` -> validates/parses JS object payload
