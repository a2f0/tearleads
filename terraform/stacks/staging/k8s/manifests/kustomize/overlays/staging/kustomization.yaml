apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Ingress is currently rendered dynamically in scripts/deploy.sh
# from manifests/ingress.yaml.
# It is intentionally left out of this initial scaffold to avoid behavior changes.
